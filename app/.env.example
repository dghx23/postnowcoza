# Postgres connection string (audit log + metadata)
DATABASE_URL="postgresql://user:password@localhost:5432/postnow"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-hex-32"
NEXTAUTH_URL="http://localhost:3000"

# S3-compatible object storage for uploaded documents (Cloudflare R2)
# S3_REGION must be the literal string "auto" for R2 - any other value
# breaks the SigV4 request signature (R2 rejects it with
# SignatureDoesNotMatch, not a scoping/latency issue like on real AWS S3).
S3_REGION="auto"
S3_ENDPOINT=""
S3_BUCKET="postnow-documents"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""

# Bob Go (bobgo.co.za) courier API - dispatch and managed returns
BOBGO_BASE_URL="https://api.sandbox.bobgo.co.za/v2"
BOBGO_API_TOKEN=""
# Generated under Account settings -> Webhook subscriptions -> Webhook secret
BOBGO_WEBHOOK_SECRET=""

# Our print/dispatch facility address - collection point for outbound
# shipments, delivery point for returns
FACILITY_STREET_ADDRESS=""
FACILITY_LOCAL_AREA=""
FACILITY_CITY=""
FACILITY_ZONE=""
FACILITY_POSTAL_CODE=""
FACILITY_CONTACT_NAME="PostNow Dispatch"
FACILITY_CONTACT_EMAIL=""
FACILITY_CONTACT_PHONE=""

# Bob Pay (bobpay.co.za) payments API - dispatch fee collection
BOBPAY_BASE_URL="https://api.sandbox.bobpay.co.za"
# JWT from POST /login, expires after 30 days - needs manual/scheduled renewal
BOBPAY_API_TOKEN=""
# Account passphrase, used to verify the `signature` field on incoming webhooks
BOBPAY_PASSPHRASE=""

# Epson Connect - one-click print queue printing (unverified integration,
# see app/src/lib/epson.ts - confirm against https://developer.epsonconnect.com/
# before relying on this in production)
EPSON_CLIENT_ID=""
EPSON_CLIENT_SECRET=""
EPSON_REDIRECT_URI="https://app.postnow.co.za/api/epson/callback"
# Required - every Epson printing/* call needs this alongside the Bearer
# device token (see app/src/lib/epson.ts)
EPSON_API_KEY=""
# Optional overrides if Epson's documented base URLs differ from the defaults
EPSON_AUTH_BASE_URL=""
EPSON_API_BASE_URL=""

# Linux Print Agent - alternative printing method to Epson Connect, for any
# CUPS-attached printer (see agent/README.md). A static bearer token, since
# the agent is an unattended script with no browser session - generate with
# `openssl rand -hex 32` and set the identical value on the agent machine.
PRINT_AGENT_TOKEN=""

# The Courier Guy direct API (api.portal.thecourierguy.co.za, Shiplogic-based)
# - dashboard quote tool only, not used for actual dispatch (see
# app/src/lib/courierguy.ts)
COURIER_GUY_API=""
# Optional override; defaults to production (https://api.portal.thecourierguy.co.za)
COURIER_GUY_BASE_URL=""

# WhatsApp Business Cloud API (Meta Graph API) - customer-facing bot for
# document tracking, courier quotes, and a staff dispatch summary
# (see app/src/lib/whatsapp.ts and app/src/pages/api/whatsapp/webhook.ts)
# Permanent/system-user access token for the WhatsApp Business app
WHATSAPP_ACCESS_TOKEN=""
# The phone number ID (not the phone number itself) from the Meta app dashboard
WHATSAPP_PHONE_NUMBER_ID=""
# Arbitrary string you choose; must match the value entered in the Meta
# webhook configuration so GET verification challenges succeed
WHATSAPP_VERIFY_TOKEN=""
# Comma-separated list of staff WhatsApp numbers (digits only, e.g.
# 27821234567) allowed to use the dash_summary menu option. Pragmatic
# first pass since User has no phone column yet - see TODO in webhook.ts
WHATSAPP_STAFF_NUMBERS=""
